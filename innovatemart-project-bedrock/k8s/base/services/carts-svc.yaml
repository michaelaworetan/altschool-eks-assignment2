apiVersion: v1
kind: Service
metadata:
  name: carts-svc
  namespace: retail-store
spec:
  selector:
    app: carts
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP